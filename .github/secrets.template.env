# GitHub Actions Secrets Template
# Copy this file and fill in the values, then add them to GitHub repository secrets
# Repository Settings > Secrets and variables > Actions > New repository secret

# ============================================
# AWS Configuration
# ============================================

# AWS region where resources are deployed
AWS_REGION=us-east-1

# IAM role ARN for staging deployments (created by setup-oidc.sh)
AWS_ROLE_ARN_STAGING=arn:aws:iam::ACCOUNT_ID:role/GitHubActions-TriviaNFT-Staging

# IAM role ARN for production deployments (created by setup-oidc.sh)
AWS_ROLE_ARN_PRODUCTION=arn:aws:iam::ACCOUNT_ID:role/GitHubActions-TriviaNFT-Production

# ============================================
# Staging Environment
# ============================================

# API Gateway URL for staging (get from CDK outputs after deployment)
STAGING_API_URL=https://api-staging.trivia-nft.example.com

# S3 bucket name for staging frontend (get from CDK outputs)
STAGING_S3_BUCKET=trivia-nft-web-staging-bucket

# CloudFront distribution ID for staging (get from CDK outputs)
STAGING_CLOUDFRONT_DISTRIBUTION_ID=E1234567890ABC

# ============================================
# Production Environment
# ============================================

# API Gateway URL for production (get from CDK outputs after deployment)
PRODUCTION_API_URL=https://api.trivia-nft.example.com

# S3 bucket name for production frontend (get from CDK outputs)
PRODUCTION_S3_BUCKET=trivia-nft-web-production-bucket

# CloudFront distribution ID for production (get from CDK outputs)
PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID=E0987654321XYZ

# ============================================
# E2E Testing (Required for CI)
# ============================================

# PostgreSQL connection string for test database
# Example: postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://user:password@localhost:5432/trivia_nft_test

# Redis connection URL
# Example: redis://default:password@host:6379
REDIS_URL=redis://default:password@localhost:6379

# Redis authentication token
REDIS_TOKEN=your-redis-token-here

# Inngest event key for workflow testing
INNGEST_EVENT_KEY=your-inngest-event-key-here

# Inngest signing key for webhook verification
INNGEST_SIGNING_KEY=your-inngest-signing-key-here

# ============================================
# Optional: Codecov (for test coverage reporting)
# ============================================

# Codecov token (get from https://codecov.io)
# CODECOV_TOKEN=your-codecov-token-here

# ============================================
# How to Add Secrets to GitHub
# ============================================
#
# 1. Go to your repository on GitHub
# 2. Click Settings > Secrets and variables > Actions
# 3. Click "New repository secret"
# 4. Enter the secret name (e.g., AWS_REGION)
# 5. Enter the secret value
# 6. Click "Add secret"
# 7. Repeat for all secrets above
#
# ============================================
# How to Get CDK Output Values
# ============================================
#
# After deploying infrastructure with CDK:
#
# 1. Check CloudFormation outputs:
#    aws cloudformation describe-stacks \
#      --stack-name TriviaNFT-WebStack-Staging \
#      --query 'Stacks[0].Outputs'
#
# 2. Or check CDK outputs in terminal after deployment
#
# 3. Update the secrets with actual values
#
